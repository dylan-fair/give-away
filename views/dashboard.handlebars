
 
          <div class="cForm-container">
               <a href="/"> &larr; Back to Home</a>
            <center>
        <h1 class="create-h1">Create New Listing</h1>
        <div class="form-floating mb-3">
            <input type="text" class="urlInput cInput" id="floatingUrl" placeholder=" Enter Url For Image">
          </div>
        <div class="form-floating mb-3">
            <input type="text" class="titleInput cInput" id="floatingTitle" placeholder=" Enter Title">
          </div>
          <div class="form-floating">
            <input type="text" class="textInput cInput" id="floatingText" placeholder=" Enter Text">
          </div>
          <div class="a-grid">
            <button type="button" class="aBtn">Create</button>
          </div>
        </center>
          </div>
          <h2>Your Posts:</h2>
  

<div class="card-container">
  {{#each items as |item|}}

{{> item-list item}}

{{/each}}
</div>

          <script src="/javascript/add-item.js"></script>